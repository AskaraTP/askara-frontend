# Backend API Endpoint (Use http://localhost:5000/api for local dev, or /api / remote url for production)
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Canonical Site URL (Used for SEO metadata, OpenGraph, and JSON-LD schemas)
NEXT_PUBLIC_SITE_URL=https://askara.co.id
